[esri_map_viz]
label = ESRI Web Map
description = Interactive map rendering ESRI web service layers (tiles, features, dynamic maps) with SPL search result overlay
default_height = 500
allow_user_selection = true
disabled = 0
search_fragment = | iplocation src_ip | table src_ip, lat, lon, City, Country | rename lat as latitude, lon as longitude
